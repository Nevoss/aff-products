export default {

  admins({ admins }) {
    return admins;
  },

  choosenAdmin({ choosenAdmin }) {
    return choosenAdmin
  },

  meta({ meta }) {
    return meta;
  },

  links({ links }) {
    return links
  },

  listLoader({ listLoader }) {
    return listLoader
  },

  singleLoader({singleLoader}) {
    return singleLoader
  },
}
