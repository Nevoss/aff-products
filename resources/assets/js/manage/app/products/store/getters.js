export default {
  products({ products }) {
    return products
  },

  choosenProduct({ choosenProduct }) {
    return choosenProduct
  },

  meta({ meta }) {
    return meta;
  },

  links({ links }) {
    return links
  },

  listLoader({ listLoader }) {
    return listLoader
  },

  singleLoader({singleLoader}) {
    return singleLoader
  },

}
